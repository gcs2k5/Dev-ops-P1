#initilaztion of Git repo in a fodler
git init

#2. Adding default branch as main
git branch -M main


#3. Adding remote repo i.e. addming public url for project  ( add, commit and push)
git remote add origin https://github.com/gcs2k5/Dev-ops-P1.git

#4. Adding files in staging area
git add file1.txt
git add .

#5. Commiting the files
git commit -m "I have added a files"

#6. Pushing the files to remote repo
git push -u origin main

#7. Viewing the branches
git branch


#8. Creanting a new branch
git branch feature/updating-codes


#9. Switching to the new branch
git checkout feature/updating-codes

#10. after adding codes in local repo, push the codes to remote repo
git add. 
git commit -m "I have added a files" 
git push -u origin feature/updating-codes 

#11. directory creating cand checking out to new branch
git checkout -b feature/updating-codes-1


git add README.md
X